#include <iostream>

//include openCV
#include <opencv2/opencv.hpp>
#include <opencv2/core/types.hpp>
#include <opencv2/core.hpp>

using namespace std;
using namespace cv;

vector myRansac(vector<Point2f> src_pts, vector<Point2f> dst_pts) {
	r = rand() % (src_pts.size() - 1);
	sx = norm)
	for (size_t i = 0; i < src_pts.size(); i++)
	{
		if(src_pts[])
	}
}